# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# FastAPI Backend — Vercel deployment deps
# Heavy ML packages (TF/torch/prophet) are
# excluded — predict endpoints fall back to
# empty arrays when models aren't available.
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Core data
yfinance>=0.2.28
pandas>=2.0.3
numpy>=1.24.4
scipy>=1.11.4
scikit-learn>=1.3.2
pyarrow>=13.0.0
statsmodels>=0.14.0

# API layer
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0
redis>=5.0.1
slowapi>=0.1.9
httpx>=0.25.2
python-multipart>=0.0.6
aiofiles>=23.2.1

# MLOps (mlflow tracking only — no training)
mlflow>=2.9.2

# Utilities
python-dotenv>=1.0.0
loguru>=0.7.2
requests>=2.31.0
joblib>=1.3.2
