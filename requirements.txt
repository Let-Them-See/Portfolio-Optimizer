# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Real-Time Stock Portfolio Optimizer
# Streamlit Cloud Requirements (dashboard only)
# Heavy ML packages (TF, torch, prophet, etc.)
# are only needed for the FastAPI backend —
# see requirements-full.txt for local dev.
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Core Data
yfinance>=0.2.28
pandas>=2.0.3
numpy>=1.24.4
pyarrow>=13.0.0

# Dashboard
streamlit>=1.28.2
plotly>=5.18.0

# API client (dashboard → FastAPI)
httpx>=0.25.2
requests>=2.31.0

# MLflow (optional — page 05 wraps in try/except)
mlflow>=2.9.2

# Utilities
python-dotenv>=1.0.0
loguru>=0.7.2
tqdm>=4.66.1

# Testing
pytest>=7.4.3
pytest-asyncio>=0.21.1
pytest-cov>=4.1.0

# Code Quality
flake8>=6.1.0
black>=23.11.0
isort>=5.12.0

# Data Validation
pydantic-settings>=2.1.0
